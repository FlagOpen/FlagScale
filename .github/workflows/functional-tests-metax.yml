# .github/workflows/create-container-functional-tests.yml
name: Functional Tests Metax

on:
  workflow_call:
    inputs:
      type:
        required: true
        type: string
      task:
        required: true
        type: string
      image:
        required: true
        type: string

jobs:
  functional-test-metax:
    runs-on: [self-hosted, metax]
    steps:
      - name: Run tests
        run: |
          echo "Running tests on metax runner"